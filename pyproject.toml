[project]
name = "pixiedust"
version = "0.1.0"
description = ""
requires-python = ">=3.10"
dependencies = [
    "rich>=14.3.4",
    "textual>=8.2.0",
]

[project.scripts]
pixie-dust = "PixieDust.cli:main"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
]
