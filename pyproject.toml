[project]
name = "pixiedust"
version = "0.1.0"
description = ""
requires-python = ">=3.10"
dependencies = [
    "click>=8.4.1",
    "rich>=14.3.3",
    "textual>=8.2.0",
    "pyyaml>=6.0.1",
]

[project.scripts]
pixie-dust = "PixieDust.cli:main"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "ty>=0.0.37",
    "ruff>=0.15.13",
]
