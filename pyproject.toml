[project]
name = "pixiedust"
version = "0.1.0"
description = ""
requires-python = ">=3.12"
dependencies = [
    "rich>=14.3.3",
    "textual>=8.2.0",
]

[project.scripts]
pixie-dust = "PixieDust.cli:main"
